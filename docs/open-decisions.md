# Open Decisions

Genuinely unsettled questions. An item leaves this list only with the artefact or decision that
resolved it — not by being forgotten.

Settled decisions live in [adr/](adr/). If something here is decided, it becomes or amends an ADR
and the row is marked `Resolved` with a pointer.

---

| ID | Question | Why it is open | Resolved by | Phase |
|---|---|---|---|---|
| ~~**OD-1**~~ | ~~Does the configured model support `response_format: json_schema`, `json_object`, or tool calling?~~ | **RESOLVED (Phase 0).** Measured through the firewall: the primary model supports `json_schema` at 5/5 schema-valid; the long-context model is served with speculative decoding and rejects every grammar-constrained mode, but supports `tool_choice: auto`. Routing is by task. | `eval/reports/20260823T091726Z__model-capabilities/report.md`; ADR-008 amended | **0 — done** |
| **OD-2** | Is self-hosted vLLM ever validated for this project? | The reference machine has 4 GB VRAM and cannot serve a useful model at the required context. vLLM is a documented deployment target with no evidence behind it. | A served model with measured latency on adequate hardware, or permanent deferral | 9+ |
| **OD-3** | Should MEDAUTH enable the firewall's provenance overlay? | The capability exists in the firewall but ships **off and uncalibrated** (its OD-3). MEDAUTH would be its first real consumer. | A calibrated threshold and an ADR **in the firewall repository** first | 8+ |
| **OD-4** | May a `DRAFT` (unreviewed) criteria tree produce recommendations outside evaluation? | Human review does not scale to a large corpus, but an unreviewed tree is an unvalidated rule set driving clinical recommendations. | ADR-007 amendment; likely `criteria_tree_reviewed` stays a hard gate feature | 2 |
| **OD-5** | Does `self_consistency` (k independent adjudications per criterion) add signal over `criteria_coverage` and `rerank_margin`? | It multiplies adjudication cost by k. Worth it only if it improves the coverage/safety frontier. | Measured on dev in Phase 6 before adoption | 6 |
| **OD-6** | Is Presidio adopted? | All data here is synthetic, so it would detect nothing real while adding spaCy, a model download and startup cost; the firewall already redacts on the model path. Shipping it would be a compliance gesture, not a control. | A real-data deployment decision. Deferred, not rejected | — |
| **OD-7** | Will a pilot with real clinical reviewers happen? | Override rate, human/AI agreement and review-time claims are **refused** without one. This is the largest gap between what the system does and what can be said about it. | A pilot with participants and a protocol | — |
| **OD-8** | What are the abstention thresholds, and what are the unsafe-rate ceilings? | Cannot be chosen before calibration data exists. Ceilings must be **pre-registered** in ADR-011 before the sweep is run. | ADR-011 amendment before Phase 6 step 2 | 6 |
| **OD-9** | Will the Kubernetes manifests ever run on a real cluster? | No cluster exists on the reference machine — no `kubectl`, `kind`, `minikube` or `helm`. Until one does, "runs on Kubernetes" is a refused claim and `kubeconform` validation is the permitted one. | A cluster run producing an artefact | 9+ |
| **OD-10** | Which embedding model and reranker? | `BAAI/bge-base-en-v1.5` and `BAAI/bge-reranker-base` are **defaults, not choices**. Coverage policy is a narrow, templated, jargon-dense domain and general-purpose retrieval quality may not transfer. | The Phase 1 retrieval comparison report; ADR-006 amended | 1 |
| **OD-11** | How many procedures should the initial corpus cover? | Too few and the evaluation measures nothing general; too many and Phase 1 never finishes. Starting point is 5–8, spanning national and jurisdictional coverage with at least one superseded version. | Phase 1 selection record, written before download | 1 |
| **OD-12** | Should the optional LLM faithfulness check be enabled by default? | It costs a call per case and can only withhold, never release. Whether it catches anything the deterministic guardrail misses is unmeasured. | Phase 6 measurement of its marginal catch rate | 6 |
| **OD-13** | How are conflicting LCDs across jurisdictions handled beyond routing to a human? | Currently row 2 → `HUMAN_REVIEW`, which is safe but may be common enough to be unhelpful. | Frequency measured in Phase 1; revisit only if material | 1, 6 |
| **OD-14** | Should MEDAUTH expose an OpenAI-compatible surface of its own? | It would make the system embeddable in other tools, but MEDAUTH is not a chat system and forcing its outputs into that shape would misrepresent them. | Deferred; no requirement exists | — |
| **OD-15** | Billing & Coding Articles declare no covered procedure, so resolution cannot reach them from a procedure code. Should an Article be resolvable through the determination it accompanies? | Found while authoring the Phase 1 retrieval set: question q012 asks which diagnosis codes support necessity, and the answer lives in an Article that no request can resolve to. Linking Articles to their parent LCD would fix it; doing so by inferring the link is exactly the kind of guess ADR-004 exists to prevent. | An explicit `accompanies` relation declared by the document, plus an ADR-004 amendment | 2 |
| ~~**OD-16**~~ | ~~Integration tests truncate a shared database.~~ | **RESOLVED (Phase 2B).** It stopped being theoretical: a test run destroyed an ingested 42 CFR corpus and the next retrieval evaluation reported 0.0000 recall. Tests now use a dedicated `medauth_test` database, with a regression test asserting the URLs differ. | `tests/integration/conftest.py` | **2B — done** |
| **OD-17** | Should Synthea supply the demographic layer of synthetic cases? | Evaluated and not used: it generates patients *first*, which reverses the required policy-to-case order; its FHIR output carries none of the decision-relevant facts; and what it would contribute is exactly the fields that must not affect the outcome. Recorded as a judgement call, not a silent omission. | A decision that demographic provenance from a named tool is worth the dependency | — |
| **OD-18** | How is the abstention gate calibrated when the gold set may be scored once? | Thresholds are selected on `development` (40 cases), which is small. Whether 40 supports a scored gate at all is unknown until the coverage-vs-accuracy sweep is run. | The Phase 6 sweep on dev; ADR-011 amendment | 6 |
| **OD-19** | Are the transcribed criteria the *right* ones? | **MEASURED, NOT RESOLVED (Phase 3).** The question now has a denominator: 356 provisions enumerated, 312 substantive, 29 carry the 33 existing criteria, 80 ruled `NON_DECISION_RELEVANT` with reasons, and **247 await qualified review**. Seven concrete gaps are confirmed by inspection, including 410.38 `(d)(1)(ii)(A)` prior-to-delivery timing and 410.32 `(b)(3)` supervision levels, which makes C03 not checkable on its own. Engineering has taken this as far as it can (R-50). | A qualified reviewer working the protocol in `docs/evaluation/ground-truth-review-protocol.md` | — |
| ~~**OD-20**~~ | ~~Should NCD/LCD material be pursued through a US-reachable route?~~ | **RESOLVED (Phase 3).** Yes for NCDs, no for LCDs. `api.coverage.cms.gov` is not behind the edge restriction that blocks `cms.gov`: NCD endpoints return `200` including version history; LCD and Article endpoints return `401` behind an AMA/ADA/AHA licence gate. No agreement was accepted and no `401` endpoint was pursued. NCDs adopted as a separate layer; LCDs deferred to OD-21. | ADR-022; `docs/data/ncd-lcd-source-assessment.md` | **3 — decided** |
| **OD-21** | Should the AMA/ADA/AHA licence terms be accepted to unlock LCDs and Billing & Coding Articles? | Opened by ADR-022. LCDs are where jurisdictional coverage criteria and procedure-level code linkage actually live, and their absence is why all 16 code links are `HUMAN_CURATED` or `INFERRED` rather than `AUTHORITATIVE`. This is **not an engineering decision**: it is a copyright-licence decision with a redistribution obligation the repository must be restructured to honour before any descriptor text could be handled. | An explicit decision by the project owner, plus a plan keeping CPT(R)/CDT(R) descriptor text out of the repository | — |
| **OD-22** | Is the retrieval evaluation set difficult enough to mean anything? | **PARTIALLY ANSWERED (Phase 4).** `retrieval_eval_v2` was built: 52 queries, 10 categories, 6 negative, 3 ambiguous, 8 historical, 4 cross-version pairs, and all 35 criteria covered. It **does** discriminate at rank 1 - Recall@1 spans 0.6429 to 0.8095 where v1's arms tied - and per-category results separate PARAPHRASED (1/5 to 3/5) and HISTORICAL_VERSION (4/8 to 7/8). It still **saturates by rank 3**: Recall@3 is 0.9762 and Recall@5 is 1.0000 in every arm. With 3-25 chunks per version the answer is nearly always in the top three, and no benchmark design fixes that. | A larger corpus - the NCD layer from ADR-022 | 5 |
| ~~**OD-23**~~ | ~~Are required criteria conjunctive for every policy?~~ | **RESOLVED (Phase 4, ADR-023).** No. Policy logic is now declared per policy version as a small three-valued expression - AND, OR, NOT, at-least-n, unless, when - evaluated under Kleene semantics so an alternative pathway can carry a case despite an unknown. 42 CFR 410.32's mammography exception is declared and tested. An undeclared policy gets an explicit `ASSUMED_CONJUNCTION`, which is the old behaviour with the assumption made visible. | ADR-023; `app/decision/logic.py`; `docs/decision/policy-logic-model.md` | **4 - done** |
| ~~**OD-24**~~ | ~~Should the four `REVIEW_REQUIRED` policy versions be routed to `HUMAN_REVIEW` at runtime?~~ | **RESOLVED (Phase 5, ADR-024).** Yes. The row framed this as an either/or - route them and force gold_v2, or leave the gap - and missed a third option: reproduce the historical assumption through a named path that production refuses. `eval/replay.py` stamps `GOLD_V1_REPLAY`; `app/policy/semantics_guard.py` refuses it on origin and on digest. gold_v1 stays byte-identical, all 222 labels reproduce, and 78 of 156 gold cases are recorded as unusable for validating production (R-63). | ADR-024; `docs/evaluation/phase5-impact.md` | **5 - done** |
| **OD-25** | Should an UNKNOWN exclusion hold a case? | Today only an evidenced exclusion denies; one whose verdict is INSUFFICIENT_EVIDENCE is treated as not excluding, so a case can reach approval with an exclusion question unanswered (R-56). An unresolved *requirement* holds the case, and the asymmetry is not obviously right. It is preserved because changing it would alter gold_v1 labels on the strength of an argument nobody has made. | A reasoned decision plus an ADR-010 amendment; changing it forces gold_v2 | — |
| **OD-26** | What does each acquired NCD actually establish about coverage? | **ADVANCED, NOT RESOLVED (Phase 6).** The machinery exists: four status origins, located evidence with a span contract, demotion of any claim its evidence cannot support, and a 19-version review queue with nothing pre-filled. 8 of 19 carry an `ENGINEERING_DERIVED` candidate from a narrow phrase scan - inadmissible as a conclusion by construction - and 11 carry none, because a scan that always produces a candidate is a classifier rather than a starting point. **No status is recorded, so the layer still establishes nothing** (R-65). | A qualified reviewer working `data/review/ncd_status_review.jsonl` | 7 |
| **OD-27** | Should NCDs be linked to procedure codes, and on whose authority? | **ADVANCED, NOT RESOLVED (Phase 6).** Yes, curated, and explicitly non-authoritative. Six links exist across three authority levels; `ENGINEERING_INFERRED` is refused by production resolution and two such links are retained deliberately so the refusal is exercised against real data. **No link is `SOURCE_STATED` and none can be** while the MCIM record publishes no procedure-code field. | A reviewer verifying the curated links, or a source that carries linkage itself (OD-21) | 7 |
| **OD-28** | Should the retrieval evaluation sets be re-run now that `ENGINEERING_INFERRED` links no longer resolve? | **SHARPENED (Phase 8).** The question is now concrete: `retrieval_v3` exists, is provenance-clean (36/36 scorable) and has **never been scored**. Scoring it is the only way to learn whether it discriminates - and the decision to score belongs BEFORE the run, because choosing afterwards between v2's numbers and v3's would be choosing on results. Until then the benchmark status is `RETRIEVAL_BENCHMARK_NOT_READY` and nothing may be tuned against any retrieval set. | A decision to score v3, recorded before the run | 9 |
| **OD-29** | Who is qualified to answer FOCUS-001, and how is that recorded? | The decision gate requires a `qualification` string and deliberately does not enumerate what counts - the project cannot list what qualifies someone to read coverage regulation, and a closed list would either exclude a legitimate reviewer or become a checkbox. So the standing is recorded and never checked, which means a later reader can weigh it and the system cannot. Whether that is sufficient, or whether a named panel is needed before a decision is ACCEPTED, is unsettled. | A decision about reviewer standing, made before the first FOCUS decision is accepted | — |
| **OD-30** | Does the first vertical slice run against a policy admitted by FOCUS-001, or against a second policy admitted some other way? | 42 CFR 410.32 is the nearest candidate and is blocked on FOCUS-001 alone. If the answer is `LEAVE_C03_NOT_ADJUDICABLE`, that policy stays inadmissible and the slice needs a different one - and no other version currently passes all eleven conditions either. Choosing after seeing the answer is legitimate; choosing *because* of it, without saying so, is not. | A recorded decision naming the policy version and the conditions it passes, made after FOCUS-001 is accepted | 10 |
| **OD-31** | Is the retrieval configuration the first slice uses ever justified, or permanently reported as unevaluated? | `RETRIEVAL_BENCHMARK_NOT_READY` does not block the slice - the slice claims nothing comparative. But a configuration nobody chose on evidence is easy to describe later as one that was. | Scoring `retrieval_v3` under OD-28, or a standing statement in every slice report that the configuration is unevaluated | 10 |

