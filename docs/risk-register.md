# Risk Register

Failure modes with evidence. An item leaves this list only with the artefact that resolved it.

**Status:** planning phase. Every risk is `Open` and unmitigated — no code exists. Likelihood and
impact are pre-implementation judgements, revised against measurement in the phase shown.

| | |
|---|---|
| **Open** | identified, not yet mitigated |
| **Mitigated** | a control exists and a test asserts it |
| **Accepted** | will not be fixed; the reason is recorded |
| **Resolved** | an artefact demonstrates it no longer applies |

---

## Clinical and domain risk

| ID | Risk | Impact | Mitigation | Status | Phase |
|---|---|---|---|---|---|
| **R-01** | **A denial is issued for missing documentation rather than for evidenced non-satisfaction.** The most common harm pattern in automated prior authorization. | Critical | Rows 6 and 8 of the decision table are separate and row 6 is evaluated first; denial requires positive, span-verified evidence of non-satisfaction. Asserted by the truth-table suite. | Open | 5 |
| **R-02** | "No applicable policy" is treated as non-coverage. | Critical | Row 1 → `NEEDS_INFO`; a test asserts `DENY_RECOMMENDED` is unreachable when resolution is empty. | Open | 5 |
| **R-03** | The wrong policy version is used — correct-looking, correctly-cited, inapplicable. | Critical | Deterministic resolution on code × jurisdiction × date of service; retrieval scoped to resolved versions; four temporal tests. | Open | 1 |
| **R-04** | **Automation bias.** Reviewers rubber-stamp, making human oversight nominal. The system's own UI can cause this. | High | Evidence rendered before the recommendation; `NEEDS_INFO`/`NO_DECISION` equally prominent; override reason enforced at the database; override rate and review duration instrumented. **Effectiveness is unmeasured** and the mitigation claim is refused. | Open | 7 |
| **R-05** | A `DENY_RECOMMENDED` is read as a determination rather than a draft rationale. | High | Denial is always human-mandatory; console framing; stricter evidence bar and gate threshold; denial precision reported separately. | Open | 5, 7 |
| **R-06** | Criteria trees are model-extracted, so a systematic extraction error becomes a systematic adjudication error. | High | Human review before evaluation use; provenance chunk ids per criterion; correction rate recorded. Extraction accuracy is **not claimed**. | Open | 2 |
| **R-07** | Coverage policy is genuinely ambiguous and a criteria tree imposes false precision. | Medium | `INFORMATIONAL` criteria for material that does not decompose; ambiguity routes to `NEEDS_INFO`/`HUMAN_REVIEW` rather than forcing a verdict. | Open | 2 |

## GenAI and grounding risk

| ID | Risk | Impact | Mitigation | Status | Phase |
|---|---|---|---|---|---|
| **R-08** | Hallucinated citation. | Critical | Deterministic span verification; failure ⇒ `NO_DECISION`. | Open | 5 |
| **R-09** | **Citation misattribution** — a real quote attributed to the wrong policy. More dangerous than fabrication because a human spot-check passes it. | Critical | Claimed metadata must match stored metadata; derived fields joined, never accepted from the model. Tracked as its own metric. | Open | 5 |
| **R-10** | Span validation is tuned too loosely to reduce `NO_DECISION` volume, quietly weakening the contract. | High | The contract is fixed in ADR-009; normalization may be fixed, the contract may not. Any change requires an ADR amendment. | Open | 5 |
| **R-11** | The model does not support reliable structured output, making closed schemas unenforceable in practice. | High | **Retired on evidence (Phase 0).** The primary model supports grammar-constrained `json_schema` at 5/5 schema-valid, so conformance is structural rather than cooperative. The long-context model uses `tool_call`, where conformance is learned - its repair rate is reported as a metric from Phase 4. | **Resolved** | 0 |
| **R-12** | Per-criterion adjudication cost or latency is impractical at realistic tree sizes. | Medium | Measured in Phase 4; independent calls parallelise; criteria per case bounded. | Open | 4 |
| **R-13** | The model refuses clinical content, or hedges into unusable output. | Medium | Detected and counted as a capability finding, not silently prompted around. | Open | 4 |

## Security risk

| ID | Risk | Impact | Mitigation | Status | Phase |
|---|---|---|---|---|---|
| **R-14** | **Indirect prompt injection via retrieved policy text, with the firewall providing little protection** (its measured indirect recall is 0.1423, planted-content recall 0.0938). | Critical | Five structural layers in MEDAUTH (fenced delivery, closed schemas, span-verified quotes, code-computed decision, per-criterion isolation). **Containment is unmeasured until Phase 8** and no containment claim is made before then. | Open | 4, 8 |
| **R-15** | Corpus poisoning — stored policy text altered or a fabricated policy inserted. | Critical | Content hashes at document and chunk level; ingest only from registered sources; historical citations re-validatable. | Open | 1, 8 |
| **R-16** | Audit tampering — selective deletion to hide a recommendation. | Critical | Append-only grants; retention deletes by `created_at` and nothing else, asserted against the compiled SQL. | Open | 7 |
| **R-17** | Clinical text leaks into logs, spans or audit payloads. | High | Redaction at the sink; audit payloads carry ids and spans; schema-level assertion that no column holds a prompt or completion. | Open | 7, 8 |
| **R-18** | The provider credential leaks. | High | MEDAUTH never holds it — the firewall does. `gitleaks` over full history in CI. **The key was shared in a planning conversation and should be rotated.** | Open | 0 |
| **R-19** | Firewall false positives block legitimate clinical notes. | Medium | Measured from Phase 0; `403` is never retried; the case routes to `HUMAN_REVIEW`. A material rate is a finding recorded in both repositories. | Open | 0 |

## Evaluation risk

| ID | Risk | Impact | Mitigation | Status | Phase |
|---|---|---|---|---|---|
| **R-20** | **Denominators too small for the differences of interest.** ~25–40 per class gives ±10–15 pp intervals. | High | Stated before any result; every per-class figure carries `n` and its interval; paired exact McNemar for comparisons; stronger statements sourced from the retrieval and grounding layers where `n` is larger. | Open | 3, 6 |
| **R-21** | Circular ground truth — a model authoring both case and label. | Critical | Labels computed by construction from the human-reviewed criteria tree, via the same decision table. | Open | 3 |
| **R-22** | Constructed cases are cleaner than real notes, so results are an upper bound. | High | **Documented as a permanent limitation.** The generalisation claim is refused. | Open (accepted) | 3 |
| **R-23** | The frozen test split is re-scored until a number improves. | High | Scoring budget in the registry; further scorings require a prior ADR; `require_tunable` raises at the library boundary. | Open | 3, 6 |
| **R-24** | Generated narratives leak their own label. | Medium | Adversarial lint for criterion and outcome vocabulary; targeted human spot-check. | Open | 3 |
| **R-25** | Thresholds are tuned on the test split. | Critical | Enforced at the library boundary, not by discipline. | Open | 6 |

## Engineering and operational risk

| ID | Risk | Impact | Mitigation | Status | Phase |
|---|---|---|---|---|---|
| **R-26** | The "LLM cannot decide" invariant decays into a convention as the code grows. | Critical | Five AST-enforced boundary rules, written in Phase 0 before there is anything to violate. | Open | 0 |
| **R-27** | Documentation drifts from code. | High | `docs/` is the source of truth; documentation updates ship in the same change; the Phase 9 sweep re-checks every claim. | Open | 9 |
| **R-28** | Audit on the request path adds material latency. | Medium | Measured and accepted — a recommendation that cannot be audited is not issued. A deliberate divergence from the firewall's ADR-029, recorded in ADR-013. | Open (accepted) | 7 |
| **R-29** | Kubernetes manifests are never validated against a real cluster. | Medium | `kubeconform` in CI; the "runs on Kubernetes" claim stays refused (OD-9). | Open (accepted) | 9 |
| **R-30** | The reference machine (4 GB VRAM, ~16 GB free RAM) cannot run the full stack with optional overlays. | Medium | Langfuse is opt-in; encoders run on CPU by default; no generative model runs locally. | Open (accepted) | 8 |
| **R-31** | Next.js adds an npm supply-chain surface to a healthcare project. | Medium | Lockfile committed, `npm audit` in CI, no runtime CDN, UI renders only backend data. | Open | 7, 9 |
| **R-32** | Corpus refresh silently changes historical case behaviour. | High | Versions are added, never edited; cases pin `corpus_snapshot_id` and `policy_version_id`; a test asserts a refresh does not change a historical resolution. | Open | 1 |

## Phase 1 findings

| ID | Risk | Impact | Mitigation | Status | Phase |
|---|---|---|---|---|---|
| **R-33** | CMS is unreachable from the development network, so the corpus is CMS-*shaped* rather than CMS. | High | Acquisition is a source adapter; real documents drop into `data/cms/` with no code change. `registry.yaml` marks every document `synthetic`, and that flag is carried into each report. **The claim that retrieval figures transfer to real CMS prose is refused.** | Open (accepted) | 1 |
| **R-34** | A wrapped prose line satisfies every lexical test for a section heading, so structure detection fills with body text masquerading as sections. | High | **Found and fixed in Phase 1.** Local headings now require structural evidence - a preceding blank line and following content - and only canonical headings are accepted without it. Asserted by `test_wrapped_prose_is_not_mistaken_for_a_heading`. | **Resolved** | 1 |
| **R-35** | A section with no sentence boundary (an enumeration or code table) produces a chunk wider than the encoder context and is silently truncated at embedding time. | High | **Found and fixed in Phase 1.** Word-boundary hard split as a last resort, never mid-word. Asserted by `test_an_unpunctuated_section_is_still_bounded`. | **Resolved** | 1 |
| **R-36** | Version scoping and the temporal predicate are redundant on the happy path, so a test exercising only that path proves neither. | **Critical** | **Found and fixed in Phase 1.** The first version of the scoping tests passed with either filter deleted. Each is now tested on the path where it alone is load-bearing, and both injections fail the suite. | **Resolved** | 1 |
| **R-37** | Billing & Coding Articles cannot be resolved to from a procedure code, so their content is unreachable. | Medium | Recorded as OD-15 and excluded from the retrieval denominator with the reason stated, rather than silently dropped. | Open | 1 |

## Data-foundation findings

| ID | Risk | Impact | Mitigation | Status | Phase |
|---|---|---|---|---|---|
| **R-38** | The whole chain is self-referential: the same author wrote the policies, declared the criteria, and built the cases from them. Internal consistency is guaranteed and is therefore not evidence. | **Critical** | Stated in the dataset card, the labelling protocol and every derived report. The generalisation claim is refused. Only real CMS documents resolve it (R-33). | Open (accepted) | Data foundation |
| **R-39** | Gold labels could drift from the runtime decision if labelling used a separate implementation. | **Critical** | `app/decision/table.py` computes both. A test recomputes all 210 labels from their criterion states; disagreement fails the build. | **Mitigated** | Data foundation |
| **R-40** | An `UNKNOWN` criterion annotated as unknown in the note would hand the system a hint no real submission contains. | High | `unknown` is an omission in every case template, and a test asserts the word does not appear in any note. | **Mitigated** | Data foundation |
| **R-41** | Partition proportions drift on small strata, starving a partition. | Medium | **Found and fixed.** Rank-percentile assignment gave development 26% and validation 5%; exact per-stratum counts replaced it. | **Resolved** | Data foundation |
| **R-42** | A retrieval query copied from the criterion it targets makes retrieval look excellent and measures string matching. | High | Content-word overlap between query and criterion source must stay below 0.6; asserted by test. | **Mitigated** | Data foundation |
| **R-43** | Editing policy text that a criterion cites would silently invalidate the criterion. | Medium | Criterion verification runs at parse time and refuses the document. Discovered by an integration test whose edit tripped it. | **Mitigated** | Data foundation |

## Phase 2B findings

| ID | Risk | Impact | Mitigation | Status | Phase |
|---|---|---|---|---|---|
| **R-44** | Structural identifiers overlap: `(i)` is both the ninth letter and the first roman numeral, so a classifier reading a marker in isolation promotes sub-paragraphs to sections. Real 42 CFR 410.38 produced two sections both named "Paragraph (i)", and a criterion citing either would have collided. | **Critical** | `app/policy/hierarchy.py` resolves depth from sequence within a known parent. `tests/unit/test_hierarchy.py` **reproduces the defect against the naive rule** before proving the tracker prevents it. | **Resolved** | 2B |
| **R-45** | Body text detected as a section heading. A wrapped line beginning with a paragraph marker became a section name, anchoring criteria to prose rather than structure. | High | A heading may not itself begin with a paragraph marker. Section paths across all documents are now real headings or honest `Paragraph (x)` labels. | **Resolved** | 2B |
| **R-46** | **Integration tests destroyed the working corpus.** They TRUNCATE the corpus tables on setup; pointed at the working database, a full test run wiped an ingested 42 CFR corpus, left fixture rows behind, and the next retrieval evaluation reported 0.0000 recall because nothing resolved. Silent in both directions. | **Critical** | Tests now use a dedicated `medauth_test` database, and a regression test asserts the two URLs differ. This was OD-16, previously deferred to Phase 9; the corpus becoming real made it bite immediately. | **Resolved** | 2B |
| **R-47** | A criterion transcribed against one revision may not exist in another. 42 CFR 410.38 was restructured in 2019 and its 2026 criteria do not locate in that text. | High | Verification is per revision and refuses a mismatch. The 2019 revision has no transcription and generates no cases; the refusal is recorded rather than worked around. | **Mitigated** | 2B |
| **R-48** | A real, authoritative policy may still not support a decision. 42 CFR 411.15 declares only exclusions, so every standalone case reached the decision table's totality guard. | Medium | Marked `EXCLUSION_OVERLAY`; excluded from case generation with the reason recorded. Its criteria remain in the inventory and retrievable. | **Mitigated** | 2B |
| **R-49** | Curated code linkage could be mistaken for CMS policy. A regulation enumerates no procedure codes, so the mapping is engineering judgement. | High | Labelled `HUMAN_CURATED_ENGINEERING_LINKAGE`, header states it is authoritative for nothing, every link carries a rationale and confidence, and low-confidence entries exist deliberately. Asserted by test. | **Mitigated** | 2B |
| **R-50** | Transcription **completeness** is unverified. Span verification proves each transcribed criterion is faithful; nothing checks that the right criteria were chosen or that none was missed. | **High** | **Quantified in Phase 3, not mitigated.** The risk now has a denominator: 356 provisions enumerated, 312 substantive, 29 carrying the 33 criteria, 80 dismissed with reasons, **247 awaiting qualified review**. Seven concrete gaps confirmed by inspection. `completeness_verified` is `false` and a test forbids it flipping without review. Engineering can go no further; only a qualified reviewer resolves it (OD-19). | **Open** | — |
| **R-51** | **A criterion may be unverifiable on its own.** 42 CFR 410.32 `(b)(3)` sets supervision levels that criterion C03 references but does not contain. | **High** | **Partially addressed (Phase 7).** (b)(3) is transcribed and span-verified as C07 (the baseline: at least general supervision), and the level definitions in (b)(3)(i)-(iii) are retrievable as evidence. **The dependency is NOT closed**: which level applies to a given test is set by the physician fee schedule's supervision indicator, which is not in 42 CFR at all, so no amount of transcribing this regulation reaches it. Recorded as `PARTIALLY_RESOLVABLE_FROM_SOURCE`, which does not permit adjudication. The reviewer question is FOCUS-001. | **Open** | — |
| **R-52** | **The decision table assumed required criteria are conjunctive; at least one policy is not.** 42 CFR 410.32(a) carries a mammography exception - an alternative route to satisfying the ordering requirement. Applied universally, the table denied a case the regulation permits. | **High** | **Mitigated for 410.32 (Phase 4, ADR-023).** Policy logic is now declared per policy as a three-valued expression; 410.32's exception is declared and a regression test proves the old conjunction denied where the new logic approves. **Not mitigated generally:** four of eight policy versions carry exception wording with no declared logic, and the runtime still assumes conjunction for them (OD-24). | **Partially mitigated** | 4 |
| **R-53** | **The retrieval evaluation set cannot fail.** 21 of 33 criteria are covered, there are no negative and no ambiguous queries, and resolution accuracy of 1.0000 reflects queries built from the linkage table itself. Recall@3 is 0.9524 across every encoder/reranker arm, so the corpus cannot discriminate between configurations. | Medium | Documented rather than papered over: no configuration was changed to chase the numbers, and a test asserts the gap stays recorded. Closing it is dataset work (OD-22) that must happen before the next measurement, not after seeing which arm it favours. | **Open** | 4 |
| **R-54** | **Gold labels are internally sound but rest on an unverified rule set.** All 156 cases audit as `REQUIRES_REVIEW`: each label is derived correctly from its criterion states by the same `decide()` the system uses, but whether those criteria are the right ones is exactly what R-50 leaves open. | **High** | The audit records the status without modifying a single case - correcting a frozen split in place would destroy the record of the experiment. Any correction ships as `gold_v2` with a recorded reason. Tests assert both the status and that the set was not touched. | **Open** | — |
| **R-55** | **No coverage determination is in the corpus.** 42 CFR states conditions of payment; it does not say whether a procedure is covered for a diagnosis in a jurisdiction, which is what prior authorization actually turns on. | **High** | ADR-022 adopts NCDs as a separate layer and defers LCDs behind the AMA/ADA/AHA licence gate (OD-21). Decided but **not implemented** - the corpus is still CFR-only, so jurisdictional resolution remains untested against real contractor policy and every local-coverage claim stays refused. | **Open** | 4 |
| **R-56** | **An UNKNOWN exclusion does not hold a case.** Only an exclusion established with valid evidence denies; one whose verdict is INSUFFICIENT_EVIDENCE is treated as not excluding, so the case can proceed to approval on an unanswered exclusion question. | Medium | **Not mitigated - preserved deliberately.** This was the pre-Phase-4 behaviour and changing it would alter gold_v1 labels on the basis of an argument nobody has made. Arguably an unresolved exclusion should hold the case as an unresolved requirement does; that asymmetry is recorded here rather than silently fixed or silently kept. | **Open** | — |
| **R-57** | **The datasets cannot detect a regression in the behaviour Phase 4 changed.** Both divergence classes require a required criterion judged NOT_SATISFIED with no valid evidence. Case generation sets `has_valid_evidence` from the criterion state, so that combination never occurs in gold_v1 or the synthetic corpus. | Medium | **Covered by unit tests, not by data.** An exhaustive old-versus-new comparison over 512,460 input combinations runs in the suite and pins both classes. The gap is that no *case* exercises the path, so a future dataset should include unevidenced verdicts explicitly. | **Open** | 6 |
| **R-58** | **No retrieval configuration is separable from its reranker.** `top_k` is 40 while the largest policy version holds 25 chunks, so first-stage retrieval never filters - every reranker receives the complete resolved scope and the encoder's ordering is discarded. Both reranked arm pairs are byte-identical across encoders. | Medium | **Reported, not tuned away.** `top_k` was deliberately not lowered after seeing the result; a corrected comparison needs a larger corpus or a pre-registered experiment at a smaller `top_k`. Until then the encoder comparison is interpretable only in the no-reranker arms. | **Open** | 6 |
| ~~**R-59**~~ | ~~The logic inventory records a doubt the runtime does not act on.~~ | **High** | **RESOLVED (Phase 5, ADR-024).** `decide()` now requires a `PolicySemantics` and the `assumed_conjunction()` fallback is deleted rather than defaulted. A `REVIEW_REQUIRED` version routes to `HUMAN_REVIEW / POLICY_SEMANTICS_UNRESOLVED`; a caller who consulted nothing gets `POLICY_SEMANTICS_UNVERIFIED`. Neither can approve or deny, proven over the full generated input space with a positive control and an anti-vacuity clause. Cost: 3 of 8 policy versions remain adjudicable. | **Resolved** | **5 - done** |
| **R-60** | **Undated coverage determinations cannot be placed in time.** The CMS Coverage API publishes prose where an effective date belongs - 14 of 24 records sampled, and 3 of 19 acquired versions. Such a determination is real and its applicability window is unknown. | **High** | **Mitigated, not solved.** Stored, indexed and provenance-preserved as `TemporalStatus.UNDATED`, and unreachable by date of service in resolution AND retrieval (one shared predicate). No sentinel date; `parse.py`'s dead `or date.min` was deleted in the same change. The limitation is the source's, and no claim of NCD temporal completeness is made. | **Mitigated** | 5 |
| **R-61** | **NCD coverage status is not recorded for any determination.** The API returns prose; classifying it COVERED or NOT_COVERED by keyword would be an unreviewed inference. | Medium | Every governing NCD resolves to `CoverageStatus.UNKNOWN` with a note saying no reviewer has recorded a status. Safe, and it means the coverage layer currently establishes nothing about any specific determination. Tracked as OD-26. | **Open** | 6 |
| ~~**R-62**~~ | ~~The retrieval evaluation runners scope by version id without a policy-type partition.~~ | Medium | **RESOLVED (Phase 6, ADR-025).** Policy type now participates in identity: `PolicyIdentity` is `(policy_type, policy_id, version)` with type first, and there is no constructor that omits it. Both eval runners and `scripts/load_code_links.py` were corrected, and the loader's whole-table DELETE - which would have wiped every CFR link when the NCD file loaded - is now scoped. An end-to-end test builds the collision with a shared code, revision id, date and byte-identical text; only policy type separates them, and each scope returns its own chunk. | **Resolved** | **6 - done** |
| **R-63** | **Half the evaluation corpus cannot validate production behaviour.** 78 of 156 gold cases and 111 of 222 synthetic cases sit on policy versions production refuses to execute. | **High** | Recorded rather than concealed: `data/policy_logic/production_coverage.json` carries the counts and a test asserts them against the datasets. gold_v1 is byte-identical and no gold_v2 was created. The usable subset is 78 gold cases on 42 CFR 410.32 and 410.61. | **Open** | — |
| **R-64** | **The logic inventory's signal scanner had a false negative.** It matched `any of the following` but not `one of the following` - the commonest way a regulation writes a disjunction. 42 CFR 410.61(b) establishes a plan "by one of the following" over five practitioner types, and `(d)(1)` has the same shape; both scanned clean and the policy was classified ASSUMED_CONJUNCTION on that basis. | **High** | **Fixed (Phase 6).** The pattern now matches `(any|one|each|all) of the following` and `whichever`. 410.61 is correctly REVIEW_REQUIRED, which dropped adjudicable policy versions from 3 to 1 and adjudicable gold cases from 78 to 26. Found by READING the regulation, not by the scan - which is why the inventory calls these search terms rather than findings, and why a clean scan is not evidence of a conjunction. | **Mitigated** | 6 |
| **R-65** | **The coverage layer establishes nothing.** 19 acquired NCD versions await status review and none has a recorded status, so every governing determination resolves to `UNKNOWN`. | Medium | **By design, not by omission.** A status model with four origins and located evidence exists, and `ENGINEERING_DERIVED` is inadmissible as a coverage conclusion by construction. Deriving a status from title, code linkage or prose is refused - it is the unreviewed inference this project exists to avoid. Resolving it is OD-26 and needs a reviewer. | **Open** | — |
| **R-66** | **No policy version is admissible for a first AI vertical slice.** The nearest, 42 CFR 410.32, fails exactly one of seven conditions: criterion C03 depends on paragraph (b)(3), which is not transcribed (R-51). | **High** | Measured by `scripts/assess_slice_admissibility.py` rather than asserted, so the answer changes when a reviewer closes the blocker. The gold impact analysis records that this decision reaches 23 of 156 gold cases. No engineering change resolves it. | **Open** | — |
| **R-67** | **Transcribing a provision silently removes it from the review queue.** Transcribing 42 CFR 410.32(b)(3) reclassified it from REQUIRES_HUMAN_REVIEW to REPRESENTED_CRITERION, dropping it out of the OD-19 package - while the dependency it blocks remained open. The question changed from "should this be a criterion?" to "does the criterion drawn from it make C03 adjudicable?", and that second question had nowhere to live. | **High** | **Mitigated (Phase 7).** A focused review package now owns the follow-on question, with exactly one priority-1 item, and `test_the_focused_review_owns_the_b3_question` asserts it. The general pattern - engineering progress retiring a review item - is not solved, only covered for this instance. | **Mitigated** | 7 |
| **R-68** | **Adjudicability was reported from policy semantics alone.** `production_coverage.json` called 42 CFR 410.32 adjudicable because its logic is DECLARED, while 23 gold cases referencing C03 would have produced a verdict on a criterion whose evidence set cannot contain the standard it is measured against. Two artefacts disagreed about the same question. | **High** | **Fixed (Phase 7).** Adjudicability is now semantics AND dependencies. The honest count fell from 1 version and 26 gold cases to **0 and 0**. The positive control moved to the axis that still has a non-zero answer, and a test asserts that executable semantics plus a blocked criterion can never be adjudicable. | **Resolved** | **7 - done** |
| **R-69** | **An unknown key in a transcription YAML was silently ignored.** A typo in `normalized_interpretation` emptied a hand-written field with no error, and nothing downstream could tell that from a criterion whose interpretation was deliberately left blank. | Medium | **Fixed (Phase 7).** The loader refuses unknown keys. Found while proving the span gate non-vacuous - three of four corruption modes were rejected and this one was accepted. | **Resolved** | **7 - done** |
| **R-70** | **Cross-version retrieval evidence now rests on one policy.** Excluding the 410.61 queries from `retrieval_eval_v3` removed one of two temporal pairs, so temporal scoping is exercised on 42 CFR 410.38 alone, and criterion coverage fell from 35 of 35 to 21 of 35. | Medium | Reported rather than repaired by restoring the inferred links, which are refused for a reason. v3 has never been scored and no v2 number may be attributed to it. Whether to re-measure is OD-28. | **Open** | 8 |
| **R-71** | **No retrieval benchmark is trustworthy enough to compare configurations on.** `retrieval_v2` discriminates (R@1 spread 0.1667) and is provenance-contaminated; `retrieval_v3` is clean (36/36 scorable) and has never been scored. Neither is usable, and they fail in opposite directions. | **High** | **Stated, not worked around.** Status is `RETRIEVAL_BENCHMARK_NOT_READY`, and while it holds nothing may be tuned against a retrieval set - not embeddings, reranker, chunking, top-k or thresholds. Nominating the least bad set would manufacture a result. Resolving it means scoring v3, which is OD-28 and must be decided before the run. | **Open** | 9 |
| **R-72** | **A domain decision could come to look answered without a reviewer.** FOCUS-001 blocks every path to a first vertical slice, which is exactly the pressure that produces a default reading as approval, a status that advances when a pipeline runs, or an "engineering acceptance". | **High** | **Mitigated structurally.** `DecisionGate.pending()` is the only constructor, submission and acceptance are separate acts each requiring attribution, only `ACCEPTED` resolves the gate, and no status name suggests provisional progress. Four mutations were injected - treating SUBMITTED as resolving, accepting an out-of-set answer, accepting a PENDING gate in one step, and ignoring the open gate at the admissibility check - and all four fail the suite. | **Mitigated** | 8 |
| **R-73** | **An impact analysis can steer a decision it exists to inform.** Presenting only what each option buys, and not what it gives up, would make the cheapest reading of a regulation look like the obvious one. | Medium | The table records costs alongside benefits for every option - `NARROW_C03_TO_BASELINE` reduces what the system checks, `SPLIT_C03` preserves it and relocates the blocker, `LEAVE_C03_NOT_ADJUDICABLE` is recorded as a legitimate answer rather than a refusal - and `OTHER` is deliberately unmodelled. A test asserts the word "recommended" appears nowhere in it. | **Mitigated** | 8 |
| **R-74** | **An external decision could be submitted and accepted by one act.** A single function turning a payload into `ACCEPTED` is one call wide, and a forged or malformed submission would then be indistinguishable from a reviewed one. | **Critical** | Submission and acceptance are separate entry points with separate attribution (`ingest_submission` cannot produce `ACCEPTED`); acceptance by the submitting identity is refused; acceptance predating submission is refused. Three mutations injected and all three fail the suite. | **Mitigated** | 9 |
| **R-75** | **An unreadable gate artefact could read as permission.** A missing, truncated or malformed `focus_001_decision.json` returning a falsy-but-not-blocking value would let a deleted file unblock production. | **Critical** | `_load()` returns `None` on any read error and `None` is `BLOCKED`; `require()` raises rather than returning a boolean a caller can ignore. Injecting a permissive read fails `test_an_unreadable_artefact_fails_closed`. | **Mitigated** | 9 |
| **R-76** | **A gold_v2 migration is the moment frozen data stops being frozen.** Editing the 23 affected cases in place looks smaller than a regeneration, and would make every committed report describe a set that no longer exists. | **High** | `app/review/migration.py` plans and creates nothing: no filesystem import at all, asserted over the AST, so both a write and its precursor fail. `IMMUTABLE` enumerates what a migration may never touch, and gold_v1's digest is asserted unchanged by planning. | **Mitigated** | 9 |
| **R-77** | **Two enums each described themselves as "the entire vocabulary".** `Verdict` (4 members) and `AssessmentState` (3) coexist at different boundaries; a slice state with no `Verdict` to map onto would surface at the first slice run rather than in the suite. | Medium | **Found in Phase 9 while updating the architecture diagram.** The narrowing is now pinned as total and injective by `test_the_slice_vocabulary_narrows_the_adjudication_vocabulary_totally`; adding a member to either enum fails it. `NOT_APPLICABLE` remains unreachable from a model. | **Mitigated** | 9 |
| **R-78** | **Contract fixtures could quietly answer the question they exist alongside.** A fixture asserting a shape for C03 would embed one reading of FOCUS-001 into the test suite, where it would later be cited as precedent. | Medium | The eight fixtures use only C01, C02 and C04; C03 is deliberately absent, and the fixture README says why. | **Mitigated** | 9 |
| **R-79** | **A relaxed control can become an invisible one.** ADR-026 permits single-party acceptance of FOCUS-001. If the exemption were inferred from the identities matching, or the marker were droppable, the record would assert an independent acceptance that never happened - a fabricated attribution in the one artefact whose entire value is attribution. | **Critical** | The exemption requires three separate declarations (explicit boolean opt-in, ADR-026 by name checked as a constant, a ≥12-word justification); `SeparationOfDuties` defaults to `TWO_PARTY` and a gate naming one identity for both acts while claiming `TWO_PARTY` is refused at construction. Six mutations injected, all six fail the suite. | **Mitigated** | 10 |
| **R-80** | **A copied template can be submitted unedited.** The payload templates carry `<<...>>` placeholders in exactly the fields that carry the record's weight - identity, rationale, justification. | Medium | Any surviving placeholder is refused by `validate_submission` and by the acceptance path, so an unfilled template cannot reach the record. | **Mitigated** | 10 |
| **R-81** | **A reviewer packet can drift from the regulation it cites.** The C08 excerpt over-ran its paragraph and trailed off mid-word in a packet a reviewer was meant to act on, and nothing failed. | High | OD-19 packets locate every quoted provision in the source at build time, with the end declared rather than inferred; a provision that moves or is reworded fails the build. A test additionally asserts every quoted line in the built packet appears verbatim in the source. Three mutations injected, all fail. | **Mitigated** | 11 |
| **R-82** | **A policy could be selected for review because its cases look easier.** Picking a target to make the gate pass is the substitution the admissibility gate exists to prevent. | Medium | The OD-19 target is derived from the gate: 42 CFR 410.33 is the only version whose failed checks are a subset of the two that declaring logic resolves. Asserted by test, so a different choice would have to change the report rather than the argument. | **Mitigated** | 11 |
| **R-83** | **A declared conjunction can over-apply a regulation's own exemption.** 42 CFR 410.33(a)(2) exempts four categories of test from every paragraph the five criteria are drawn from, and none of the four is transcribed - so the exemption cannot be expressed and a plain conjunction would deny a case the regulation exempts. | **High** | Unreachable rather than mitigated: resolution is deterministic by procedure code and R0075 - portable x-ray transport - is the only code linked to this version. Recorded as an UNRESOLVED review note in the declaration and asserted by test, so linking a mammography or audiology code fails the build before it ships (OD-34). | **Contained, not eliminated** | 11 |
| **R-84** | **A safety test can read a sentence instead of counting calls.** `test_a_blocked_request_is_never_retried` asserted the remedy text said "never retried"; a mutation that changed the prose while leaving a retry loop intact passed it. | High | **Found by injection during Phase 11.** Rewritten to assert behaviour - exactly one gateway call, parameterised over every non-retryable outcome - and re-proven against a mutation that adds an actual retry. | **Resolved** | 11 |
| **R-85** | **A fixture-latency figure can be read as a production one.** The slice reports per-stage timings from a run in which every model response came from a fixture. | Medium | The committed report carries `model_calls: 0` and a `note` field stating the latencies are the cost of the pipeline and not of inference; the evaluation doc repeats it and refuses the production claim outright. | **Mitigated** | 11 |
| **R-86** | **Grammar-constrained decoding does not guarantee termination.** JSON permits arbitrary whitespace between tokens, so a constrained decoder can satisfy the schema forever without closing the document. Observed live: 2000 completion tokens, **98.6% whitespace**, document never closed, 3 x 60 s timeout, no result. | **High** | Two structural fixes - a model-facing schema containing only fields the model can produce (`IntakeExtraction`), and a `max_output_tokens` ceiling - plus a compact-output instruction. **Mitigated, not eliminated**: the instruction took intake from 0/3 to 3/3 in isolation and one call in the live matrix still ran away at 20.8 s. It relies on the model honouring an instruction, which is the one thing a grammar constraint exists not to rely on. A decoder-side stop condition is the real fix and is not ours to make. | **Contained, not eliminated** | 12 |
| **R-87** | **A capability result is a claim about a decoder AND a schema.** The probe measured one small schema and reported `json_schema` 5/5; the same model on `IntakeResult` fails to terminate. A blanket "supports strict json_schema" would be broader than its evidence. | Medium | The capability document now states the scope explicitly and requires any future claim to name the schema it was measured against. | **Mitigated** | 12 |
| **R-88** | **The fence delimiter is confusable with an evidence id.** The first live call returned `evidence_ids: ["MEDAUTH-DATA-8f2a"]` - the model cited the fence token itself. | Low | Contained by construction: an id outside the criterion's evidence set is dropped, and a decided assessment left with nothing becomes UNKNOWN. The confusability remains and would be removed by a fence that does not resemble the `[E1]` id syntax. | **Contained** | 12 |
| ~~**R-89**~~ | ~~**The slice never detects contradictory verdicts.**~~ `GuardrailState.PASSED` is passed unconditionally, so decision-table row 5 and the `CONTRADICTORY_EVIDENCE` abstention state are unreachable from the slice. The live contradictory-note case reached NEEDS_INFO via row 6, which is safe but is not the row that was designed for it. | Medium | **Found during Phase 12 live matrix.** Not fixed: wiring contradiction detection is a design step, and doing it while reporting on a live run would mean reporting on code the run did not use. | **RESOLVED (Phase 13).** `app/guardrail/contradiction.py` runs as a stage; `guardrail_state_for()` maps its result onto the table, so row 5 is genuinely reachable. Three mutations catch a regression. The detector reads STRUCTURED evidence only - it never re-reads the note, because a detector that inferred clinical content would be adjudicating the adjudicator. | **Resolved** | 13 |
| **R-90** | **A contradiction detector that fires on ordinary cases gets switched off.** The first implementation compared `evidence_ids` across criteria - but those are assigned PER CRITERION, so `E1` for one criterion is a different passage from `E1` for another. It compared labels, not sources, and fired on every fixture case. | High | **Found immediately by the existing slice tests.** It compares chunk ids now, and only when the source sets are IDENTICAL - overlap is normal and flagging it would make the detector useless. Half the contradiction tests exist to prove it stays quiet. | **Mitigated** | 13 |
| **R-91** | **retrieval_v3 cannot separate any two arms.** 31 paired observations; the best comparison is 5 discordant queries all favouring one arm, and exact McNemar on 5-0 gives p=0.0625 - **the minimum achievable p at this sample size.** No configuration can be empirically selected on it. | Medium | Recorded as `RETRIEVAL_CONFIGURATION_UNRESOLVED`; the adopted reranker is labelled an ENGINEERING DEFAULT and the distinction is stated in the claims ledger. Resolving it needs a larger clean set, pre-registered. | **Open** | 13 |
| **R-92** | **The encoder comparison is structurally vacuous at `top_k=40`.** Both encoders produced IDENTICAL results on every query under either reranker, because `top_k` exceeds the largest scope so first-stage retrieval ranks nothing away. | Medium | Documented rather than fixed: lowering `top_k` now, having seen the result, is the tuning-after-the-fact this regime forbids. It belongs in a pre-registered protocol. | **Open** | 13 |
| ~~**R-93**~~ | ~~**The slice does not resolve policy applicability.**~~ It was handed a `PolicyIdentity` and asserted `ResolutionState(RESOLVED)`, so decision-table row 1 - no applicable policy - was unreachable and every case was adjudicated against a policy that might not apply. | **Critical** | **Found by the frozen 410.33 evaluation.** All three `POLICY_NOT_APPLICABLE` cases were mis-handled: two reached the right answer by the wrong row, and CASE-0073 produced a **DENIAL with 7 verified citations and zero citation failures** - exactly the ADR-004 failure that every grounding metric passes. **RESOLVED (Phase 15, ADR-028).** Applicability is a runtime stage running BEFORE intake, in SQL, over six states, and only `RESOLVED` continues; a `PRODUCTION` runner without an `ApplicabilityPort` cannot be constructed. `tests/integration/test_case_0073_regression.py` reconstructs the exact Phase-14 conditions - real chunks, evidenced NOT_SATISFIED verdicts, citations that verify - and four mutations prove it load-bearing. **What it does not fix: R-97.** | **Resolved** | 15 |
| **R-94** | **R-86 removes a large fraction of any real evaluation.** 10 of 26 cases (38%, CI 0.2243-0.5747) never reached adjudication; the whitespace mitigation holds on short notes and fails on 652-character clinical notes. | **High** | Fail-closed held perfectly - all 10 routed to HUMAN_REVIEW, none became a recommendation, none was excluded from the denominator. But the surviving 16 are not a random sample, so no rate from this run is an unbiased estimate. Escalated, not fixed. **Phase 15 confirmed the mechanism directly**: the identical request shape pads at 0.93 whitespace on the longest gold notes and 0.09 on the shortest, and the reduced probe Phase 13 diagnosed with no longer pads at all - so the schema was never the axis, the input length is. **The consequence is now structural**: `provider-failure-validity.v1` (ADR-028) classifies any experiment above a 10% failure rate as `DEGRADED_BY_PROVIDER_FAILURE`, by code, so the caveat travels with the number instead of living in prose beside it. | **Open** | 15 |
| **R-95** | **Criterion errors run in the unsafe direction.** `NOT_SATISFIED` recall is 0.1429: the model returned SATISFIED for 10 of 14 criteria the gold set says were not met. | **High** | No wrong approval resulted, because other criteria returned UNKNOWN and row 6 fired first - the safety property held **by the ordering, not by the model**. Remove one UNKNOWN and those become approvals. | **Open** | 14 |
| **R-96** | **An accuracy metric can count a right answer reached by the wrong rule.** Two `POLICY_NOT_APPLICABLE` cases scored as correct while firing row 6 instead of row 1. | Medium | The scorer now reports `accuracy_right_for_the_right_reason` beside `accuracy` (3/26 vs 6/26) and emits a failure record for every outcome-correct-wrong-row case. | **Mitigated** | 14 |
| **R-97** | **gold_v1's `POLICY_NOT_APPLICABLE` cases are unreachable from their own input.** The generator wrote non-applicability into the clinical NARRATIVE ("Requested service: unlisted procedure 99199") while emitting the covered code `R0075` in `requested_procedure.code`. Deterministic resolution reads the structured request and correctly RESOLVES 42 CFR 410.33, so the gold label of row 1 cannot be reached for CASE-0071, CASE-0073 and CASE-0074. | **High** | **Found in Phase 15 the moment applicability became real** - it was invisible while the runtime asserted RESOLVED, because a stage that never runs cannot disagree with a label. NOT worked around: a resolver that read the note to recover the intended answer would be a semantic resolver wearing a deterministic one's clothes, and would resolve differently for the same structured request depending on prose (ADR-004). The three cases are scored, counted and reported as `DATASET_DEFECT` failure records; none is excluded. Fixing it needs a **gold_v2** whose not-applicable cases carry a code the corpus does not cover, reproducing the split rule byte-for-byte. gold_v1 is frozen and is not edited. | **Open** | 15 |
| **R-98** | **A right answer can now be reached by a NEW wrong row.** With applicability real, a case can be refused for the wrong *kind* of absence - `TEMPORALLY_UNRESOLVED` where the label expects `NO_APPLICABLE_POLICY` - and outcome-level accuracy would score it correct. R-96 was this shape at the decision table; closing R-93 reproduces it one stage earlier. | Medium | The scorer reports `accuracy_right_for_the_right_reason` beside `accuracy` and emits a failure record whenever the rows differ, so an applicability-row disagreement appears as a failure even when the outcome matches. Per-case records carry `applicability_state` and `applicability_reason`, so which refusal fired is checkable rather than inferred. | **Mitigated** | 15 |
